# This file will contain your encrypted secrets
# Configure SOPS first by following the instructions in .sops.yaml
# Then you can add secrets like:
# my-secret: my-value
# Run: sops secrets/secrets.yaml to edit encrypted secrets
